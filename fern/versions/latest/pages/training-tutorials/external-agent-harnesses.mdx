---
title: "Training on External Agent Harnesses"
description: "Capture exact token ids and log probabilities from a harness you configure but cannot observe."
position: 6
---

Some agent harnesses run outside NeMo Gym. You can point them at your model endpoint and set their
system prompt and tools, but you cannot see their individual model calls, and the transcript they
return carries no token ids. The Claude Code CLI is the reference case: a subprocess running its own
multi-turn loop, speaking Anthropic Messages over server-sent events.

RL trains on token ids. Reconstructing them by re-tokenizing the returned text produces a sequence
that differs from what the policy actually sampled, by an amount nobody measured. Token capture
solves this by recording the exact ids at the model server, where they still exist, and rebuilding a
rollout's model calls into one contiguous response.

<Note>
You do not need this for agents that run inside Gym. A native agent calls the model server directly
and carries token ids on its own response items.

</Note>

## When you need it

| Your agent | What you need |
|---|---|
| Runs inside Gym, returns Responses items with token ids | Nothing. Train as usual. |
| Runs as a subprocess or remote service, returns text | Token capture, described below. |
| Returns token ids in a format Gym does not read | Token capture, and open an issue so the format can be read directly. |

## Turning it on

Three settings are required. Omit any one and the run completes without error while teaching the
model nothing.

**1. Enable capture and give it node-local storage.** Writer and reader are on the same node, so a
shared filesystem adds latency for nothing and lets two shards write the same file.

```yaml
env:
  nemo_gym:
    token_id_capture_enabled: true
    token_id_capture_dir: /tmp/nemo_gym_token_id_captures
```

**2. Opt the agent in.** The per-agent flag scopes capture to harnesses that need it, so a native
agent in the same run is left alone.

```yaml
responses_api_agents:
  claude_code_agent:
    token_id_capture: true
```

**3. Configure the inference server** so the ids exist and the harness's tool calls are parsed. The
second setting matters more than it looks: without a tool parser the model's tool-call syntax arrives
as ordinary text, every rollout becomes single-turn, and the run still reports success.

```yaml
policy:
  generation:
    vllm_cfg:
      skip_tokenizer_init: false
      http_server_serving_chat_kwargs:
        tool_parser: hermes
```

Sampling parameters also have to be pinned server-side. The harness chooses its own, and on-policy
training requires generation to match the sampling distribution the policy is optimized under. Set
`sampling_overrides` on the model server to your trainer's generation config.

## What you get back

Each rollout's model calls are stitched into a single Responses payload whose `output` items are
contiguous: every item's `prompt_token_ids` is the running sequence, and its `generation_token_ids`
is what the policy sampled at that step. Gym replaces the rollout's `response.output` with these
items, so a trainer reads `response.output` the same way for a native agent and an external harness.

The loss mask follows from that structure rather than being shipped separately. Prompt positions are
context, generation positions are trainable.

## What to watch on a first run

Read these before the reward curve. A rollout can look healthy with a moving reward while most of it
was never trained on.

| Metric | Expect | If it is wrong |
|---|---|---|
| `rebuilt_fraction` | 1.0 | Correlation broke. Nothing is being captured. |
| `calls_per_rollout_mean` | above 1 | The harness never called a tool. Usually a missing tool parser. |
| `delivered_fraction_mean` | near 1.0 | Chains are breaking. Part of each rollout is not reaching the optimizer. |
| `masked_rollouts` | 0 | A rollout was dropped rather than trained on partial data. |
| `empty_generation_calls` | 0 | The output budget or a content filter is truncating generations. |

`calls_per_rollout_mean` deserves particular attention. A value of exactly 1.0 means the agentic path
was never exercised, and every other metric will look correct.

## Integrating a training framework

Gym owns the record shape and the code that builds a record. Where the record goes is yours.

### The interfaces

Two protocols in `nemo_gym.token_id_capture.protocols`. The module deliberately imports no web
framework, cluster runtime or tensor library, so an inference worker can import it without pulling in
Gym's server stack.

```python
class TokenSink(Protocol):
    async def put(self, entry: TokenEntry) -> None: ...

class TokenSource(Protocol):
    async def tokens_for(self, rollout_id: str) -> list[TokenEntry]: ...
    async def drop(self, rollout_id: str) -> None: ...
```

`put` must be durable before it returns. A reader that runs after the rollout has to see the record,
and that guarantee is what allows records to be deleted once consumed.

A transport with no delete operation implements `drop` as a no-op and lets whoever owns the storage
retire the records.

### Writing somewhere other than Gym's file store

Call `install_token_sink` once at process startup. The capture path itself does not change.

```python
from nemo_gym.token_id_capture import install_token_sink

install_token_sink(MyDataPlaneSink(client))
```

This is the configuration to use when your framework owns the inference worker. The model server
still assembles the record, but the token arrays go to your transport instead of to disk, and never
ride back through an HTTP response.

Leaving `token_id_capture_enabled` off is a different thing entirely: it disables capture, so an
external harness yields rollouts with no token ids and nothing to train on. Use that only for
evaluation.

### Reading records back

`TokenCaptureStore` implements both protocols and is the default. `LocalTokenReader` reads the files
directly; `HttpTokenReader` reads over the model server's token route, for a reader that cannot see
the writer's filesystem.

The read route serves a rollout's raw training tokens on the same server the harness calls to
generate. Inside a trusted cluster that is fine. Once the harness runs in a sandbox whose only egress
is that server, it is not: the harness could read its own training data or another rollout's. Set
`token_id_capture_read_token` to require a bearer token. When it is unset the route stays open and
logs a warning at startup.

## Extensions

### Prefix supply

By default the inference engine builds each prompt by re-rendering the whole conversation through the
chat template. That can produce a different sequence than the model sampled. A tool-call parser
truncates the assistant turn at the tool call and discards anything generated after it; a chat
template can re-tokenize an assistant turn differently; a reasoning model's template may drop earlier
thinking entirely. In each case the new prompt no longer extends the previous one and the rollout
cannot be chained.

Prefix supply sends the engine the exact tokens of the call being continued, so the new prompt
extends the previous one by construction. Enable it by overlaying
`responses_api_models/vllm_model/configs/vllm_model_supply_prefix.yaml`.

It requires a backend that honours `required_prefix_token_ids` on both the chat and the tokenize
endpoints, and it only applies when exactly one recorded call matches the incoming conversation.
Otherwise the request is forwarded unchanged and starts a new chain, which costs a cold cache but is
never wrong. Each record carries whether its prefix was supplied, so the rate is auditable after a run.

### Sampling pin

`sampling_overrides` on the model server forces the sampling parameters on every request, overriding
whatever the harness sent. Generation KL error is the metric that tells you whether it is working.

## Limitations

**One trajectory per rollout.** A harness that forks sub-agents or retries a call produces a tree of
model calls. Gym delivers the chain carrying the most sampled tokens and reports the rest through
`delivered_fraction`, rather than dropping it silently. Training on the full tree needs a trainer
contract that accepts one.

**Harness calls outside the rollout.** A harness may generate a conversation title or a
context-compaction summary. Those are real policy output and are currently trained on. A compaction
summary is long enough that it can outweigh the rollout it summarizes, which shows up as
`chains_per_rollout_mean` above 1 and `delivered_fraction_mean` below 1.0.

**Multiple server workers.** The index that identifies which call a request continues is per process.
With more than one worker, calls from one rollout can be handled by different workers, and those
calls fall back to inferring the parent from token prefixes. Inference is correct but cannot
disambiguate a retry, and prefix supply does not apply to them.
